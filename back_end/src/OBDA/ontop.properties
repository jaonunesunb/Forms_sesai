# Database connection used by Ontop
jdbc.url=jdbc:postgresql://localhost:5432/postgres
jdbc.driver=org.postgresql.Driver
jdbc.user=postgres
jdbc.password=postgres

# Optional default result limit (-1 means no limit)
ontop.query.default-limit=-1
