services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pedro
      POSTGRES_PASSWORD: solo01
      POSTGRES_DB: forms
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pedro -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 15

  arango:
    image: arangodb:3.11
    environment:
      ARANGO_ROOT_PASSWORD: pwd
    ports:
      - "8529:8529"
    # (opcional) healthcheck simples – pode omitir se preferir
    # healthcheck:
    #   test: ["CMD-SHELL", "wget -q -O- http://localhost:8529/_admin/server/availability >/dev/null 2>&1 || exit 1"]
    #   interval: 5s
    #   timeout: 5s
    #   retries: 30

  backend:
    build: ./back_end
    environment:
      POSTGRES_DSN: postgresql://pedro:solo01@postgres:5432/forms
      ARANGO_URL: http://arango:8529
      ARANGO_DB: owl_db
      ARANGO_USER: root
      ARANGO_PASSWORD: pwd
      ALLOWED_ORIGINS: http://localhost:3000,http://frontend:3000
    depends_on:
      postgres:
        condition: service_healthy
      # arango:  # habilite se fizer healthcheck no arango
      #   condition: service_healthy
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./front_end
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./front_end:/app
    depends_on:
      - backend
